# UX设计文档

文档编号：`DES-UX-001`  
版本：`v0.1`  
设计负责人：`<姓名>`  
目标平台：`Web/iOS/Android/桌面/其他`  
原型位置：`<链接>`

## 1. 体验问题与目标

| 用户问题 | 受影响用户 | 当前证据 | 体验目标 | 衡量方式 |
|---|---|---|---|---|
| `<问题>` | `<用户>` | `<研究/数据>` | `<目标>` | `<指标>` |

## 2. 用户与使用情境

| 用户类型 | 目标 | 熟练度 | 设备/环境 | 限制与无障碍需求 |
|---|---|---|---|---|
| `<用户>` | `<目标>` | `<级别>` | `<环境>` | `<需求>` |

## 3. 用户研究摘要

- 研究方法：`<访谈/观察/数据分析等>`
- 样本与限制：`<说明>`
- 关键发现：`<发现>`
- 设计机会：`<机会>`

## 4. 信息架构

描述内容层级、导航模型、命名体系、搜索和信息发现方式，并附站点地图或结构图。

## 5. 用户任务流程

### `<任务名称>`

- 触发点：`<触发>`
- 成功路径：`<步骤>`
- 替代路径：`<路径>`
- 中断恢复：`<恢复方式>`
- 成功状态：`<可观察结果>`

## 6. 页面与视图清单

| 页面编号 | 页面 | 用户目标 | 入口 | 关键动作 | 权限 |
|---|---|---|---|---|---|
| `UX-P001` | `<名称>` | `<目标>` | `<入口>` | `<动作>` | `<角色>` |

## 7. 单页面设计说明

### UX-P001 `<页面名称>`

- 页面目标：`<目标>`
- 内容优先级：`<顺序>`
- 布局规则：`<规则>`
- 主要操作：`<操作及反馈>`
- 次要操作：`<操作及反馈>`
- 数据展示：`<字段和格式>`
- 校验规则：`<规则>`
- 原型引用：`<链接>`

## 8. 状态与反馈

| 场景 | 加载 | 空状态 | 错误 | 权限不足 | 离线/超时 | 恢复动作 |
|---|---|---|---|---|---|---|
| `<页面/组件>` | `<表现>` | `<表现>` | `<表现>` | `<表现>` | `<表现>` | `<动作>` |

## 9. 交互规则

描述焦点、键盘、触控、拖放、确认、撤销、批量操作、长任务进度和防重复提交。

## 10. 内容与文案

| 场景 | 文案 | 语气 | 动作标签 | 国际化注意事项 |
|---|---|---|---|---|
| `<场景>` | `<文本>` | `<语气>` | `<标签>` | `<说明>` |

## 11. 响应式与多端适配

| 断点/设备 | 导航 | 布局 | 内容优先级 | 输入方式 | 特殊限制 |
|---|---|---|---|---|---|
| `<范围>` | `<方式>` | `<规则>` | `<规则>` | `<方式>` | `<限制>` |

## 12. 可访问性

检查语义结构、键盘可达、焦点顺序、文本对比、替代文本、表单标签、错误提示、缩放和辅助技术。

## 13. 设计系统与组件

| 组件 | 变体 | 状态 | 使用规则 | 设计令牌 | 开发映射 |
|---|---|---|---|---|---|
| `<组件>` | `<变体>` | `<状态>` | `<规则>` | `<令牌>` | `<组件名>` |

## 14. 可用性验证

| 任务 | 参与者 | 成功标准 | 结果 | 发现 | 设计调整 |
|---|---|---|---|---|---|
| `<任务>` | `<样本>` | `<标准>` | `<结果>` | `<发现>` | `<调整>` |

## 15. 开发交付

列出设计文件版本、资源、组件状态、交互规格、验收方式、未决事项和设计走查安排。

## 16. 需求追溯

| 用户故事 | 用户流程 | 页面/组件 | 验收场景 | 状态 |
|---|---|---|---|---|
| `US-001` | `<流程>` | `UX-P001` | `<场景>` | 已覆盖 |

