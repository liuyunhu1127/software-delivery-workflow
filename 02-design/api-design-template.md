# API设计规范

文档编号：`DES-API-001`  
版本：`v0.1`  
API负责人：`<姓名>`  
OpenAPI版本：`<3.1.x/3.2.x>`  
服务名称：`<服务>`

## 1. API目标与边界

- 消费者：`<客户端/服务/合作方>`
- 业务能力：`<能力>`
- 不支持的用例：`<说明>`
- 契约文件位置：`<openapi.yaml路径>`

当本阶段存在HTTP API时，除本文外必须生成同目录机器可读`openapi.yaml`或`openapi.json`，使用OpenAPI 3.x并可由Swagger UI/Swagger Editor读取。机器契约是本文的配套产物，不单独评分；两者的路径、Schema、认证、错误和示例必须一致。

## 2. 基础约定

| 项目 | 约定 |
|---|---|
| Base URL | `<URL规则>` |
| 传输协议 | HTTPS |
| 数据格式 | JSON/其他 |
| 时间格式 | RFC 3339/项目约定 |
| 字符编码 | UTF-8 |
| 关联ID | `<请求头>` |

## 3. 认证与授权

| 调用方 | 认证方式 | 授权模型 | Scope/角色 | 凭据生命周期 |
|---|---|---|---|---|
| `<调用方>` | `<OAuth2/mTLS等>` | `<模型>` | `<范围>` | `<策略>` |

## 4. 资源和命名规范

定义资源名称、路径、字段、枚举、布尔值、日期、金额、ID和大小写规则。

## 5. 接口清单

| 接口编号 | 方法 | 路径 | 目的 | 权限 | 幂等 | 关联需求 |
|---|---|---|---|---|---|---|
| `API-001` | POST | `/resources` | `<目的>` | `<Scope>` | 是 | `FR-001` |

## 6. 单接口契约

### API-001 `<接口名称>`

- 业务说明：`<说明>`
- 前置条件：`<条件>`
- 请求头：`<字段与规则>`
- 路径参数：`<Schema、必填、约束>`
- 查询参数：`<Schema、必填、约束>`
- 请求体：`<Schema与示例>`
- 成功响应：`<状态码、Schema和示例>`
- 错误响应：`<状态码、错误码和示例>`
- 授权规则：`<主体、资源、动作>`
- 幂等策略：`<Key、有效期、重复请求响应>`
- 审计与指标：`<字段、指标>`

## 7. 数据Schema

| Schema | 字段 | 类型 | 必填 | 约束 | 敏感性 | 示例 |
|---|---|---|---|---|---|---|
| `<名称>` | `<字段>` | `<类型>` | 是/否 | `<约束>` | `<级别>` | `<值>` |

## 8. 错误模型

| 错误码 | HTTP状态 | 场景 | 是否可重试 | 客户端动作 | 日志级别 |
|---|---:|---|---|---|---|
| `<CODE>` | 400 | `<场景>` | 否 | `<动作>` | WARN |

## 9. 分页、排序与过滤

定义游标或页码策略、稳定排序、最大页大小、过滤操作符和无效查询处理。

## 10. 超时、重试与限流

| 场景 | 超时 | 重试条件 | 最大次数 | 退避 | 限流策略 |
|---|---:|---|---:|---|---|
| `<场景>` | `<ms>` | `<条件>` | `<次数>` | `<策略>` | `<策略>` |

## 11. 异步任务、事件与回调

描述任务状态、轮询、Webhook签名、事件Schema、投递语义、顺序和重复处理。

## 12. 版本与兼容性

说明版本策略、兼容变更、破坏性变更、弃用通知、迁移窗口和消费者验证。

## 13. 安全检查

覆盖对象级和功能级授权、输入校验、注入防护、敏感数据泄露、速率限制和审计。

## 14. 契约验证

| 验证项 | 工具/方法 | 环境 | 通过条件 | 证据 |
|---|---|---|---|---|
| Schema校验 | `<工具>` | CI | 无错误 | `<链接>` |

## 15. 变更记录

| API | 版本 | 变更 | 兼容性 | 消费者影响 | 问题/需求编号 |
|---|---|---|---|---|---|
| `API-001` | `<版本>` | `<内容>` | 兼容/破坏 | `<影响>` | `<编号>` |
