# 数据库设计文档

文档编号：`DES-DB-001`  
版本：`v0.1`  
数据库负责人：`<姓名>`  
数据库产品/版本：`<MySQL 8.4等>`  
字符集与排序规则：`<值>`

## 1. 设计范围与数据域

| 数据域 | 所有者 | 主要实体 | 数据来源 | 消费者 |
|---|---|---|---|---|
| `<域>` | `<团队>` | `<实体>` | `<来源>` | `<对象>` |

## 2. 概念数据模型

描述业务实体、关系、聚合边界和关键业务规则，并附概念ER图。

## 3. 逻辑数据模型

| 实体 | 业务键 | 属性摘要 | 关系 | 完整性规则 |
|---|---|---|---|---|
| `<实体>` | `<键>` | `<属性>` | `<关系>` | `<规则>` |

## 4. 物理模型与表清单

| 表名 | 用途 | 主键 | 预计数据量 | 增长率 | 保留期限 |
|---|---|---|---:|---:|---|
| `<table>` | `<用途>` | `<PK>` | `<数量>` | `<比例>` | `<期限>` |

## 5. 表结构定义

### `<table_name>`

| 字段 | 类型 | 可空 | 默认值 | 说明 | 敏感级别 | 约束 |
|---|---|---|---|---|---|---|
| `id` | `<类型>` | 否 |  | `<说明>` | 普通 | PK |

## 6. 主键、外键与约束

| 约束名 | 表 | 类型 | 字段 | 引用/规则 | 删除更新行为 |
|---|---|---|---|---|---|
| `<名称>` | `<表>` | PK/FK/UK/CHECK | `<字段>` | `<规则>` | `<行为>` |

## 7. 索引设计

| 索引 | 表 | 字段顺序 | 类型 | 支持查询 | 选择性 | 写入影响 |
|---|---|---|---|---|---|---|
| `<索引>` | `<表>` | `<字段>` | BTREE等 | `<查询>` | `<估计>` | `<影响>` |

## 8. 查询与访问模式

| 场景 | SQL/访问模式 | 频率 | 目标耗时 | 预计行数 | 执行计划证据 |
|---|---|---:|---:|---:|---|
| `<场景>` | `<摘要>` | `<QPS>` | `<ms>` | `<行>` | `<链接>` |

## 9. 事务与一致性

描述事务边界、隔离级别、锁顺序、死锁处理、跨服务一致性、幂等和补偿。

## 10. 分区、分片与归档

| 对象 | 策略 | 分区/分片键 | 路由 | 扩容 | 归档与删除 |
|---|---|---|---|---|---|
| `<表>` | `<策略>` | `<键>` | `<方式>` | `<方案>` | `<方案>` |

## 11. 数据生命周期与隐私

说明采集、使用、共享、保留、脱敏、匿名化、导出和删除策略。

## 12. 用户、角色与权限

| 账户/角色 | 用途 | 权限 | 作用范围 | 凭据管理 | 审计 |
|---|---|---|---|---|---|
| `<角色>` | `<用途>` | `<最小权限>` | `<库/表>` | `<策略>` | `<要求>` |

## 13. 备份、恢复与容灾

| 数据集 | RPO | RTO | 备份方式 | 恢复验证 | 异地/副本策略 |
|---|---:|---:|---|---|---|
| `<数据>` | `<时间>` | `<时间>` | `<方式>` | `<频率>` | `<策略>` |

## 14. 迁移与回滚

| 迁移编号 | 变更 | 兼容策略 | 执行时长 | 锁风险 | 回滚/前滚方案 |
|---|---|---|---:|---|---|
| `MIG-001` | `<变更>` | `<策略>` | `<估计>` | `<风险>` | `<方案>` |

## 15. 容量规划与监控

定义容量阈值、慢查询、连接、锁、复制延迟、磁盘和备份状态的指标与告警。

## 16. 数据需求追溯

| 表/字段 | 关联需求 | 关联API | 关联迁移 | 关联测试 |
|---|---|---|---|---|
| `<对象>` | `<编号>` | `<编号>` | `<编号>` | `<编号>` |

