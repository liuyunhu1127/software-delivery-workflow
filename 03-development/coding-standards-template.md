# 编码规范

文档编号：`DEV-STD-001`  
版本：`v0.1`  
技术负责人：`<姓名>`  
适用语言/版本：`<语言及版本>`  
适用仓库：`<仓库>`

## 1. 目的、范围与优先级

说明适用代码、生成代码、第三方代码和例外范围。发生冲突时明确“自动格式化配置 → 项目规范 → 语言社区规范”的优先级。

## 2. 工程与目录结构

| 路径 | 内容 | 允许依赖 | 禁止依赖 | 所有者 |
|---|---|---|---|---|
| `<path>` | `<内容>` | `<模块>` | `<模块>` | `<团队>` |

## 3. 命名规范

| 对象 | 规则 | 正例 | 反例 |
|---|---|---|---|
| 类型/函数/变量/常量等 | `<规则>` | `<正例>` | `<反例>` |

## 4. 格式与风格

定义格式化工具、配置文件、导入顺序、行长、空白、文件编码和CI校验方式。能由工具强制的规则不依赖人工记忆。

## 5. 模块和依赖

说明模块边界、依赖方向、依赖注入、循环依赖、第三方库引入、升级和许可检查。

## 6. API与数据模型实现

规定契约优先、输入输出类型、兼容性、序列化、验证、时区、金额、精度和空值处理。

## 7. 异常处理

| 层级 | 可抛异常 | 捕获责任 | 对外映射 | 重试 | 日志要求 |
|---|---|---|---|---|---|
| `<层>` | `<类型>` | `<模块>` | `<错误码>` | `<策略>` | `<要求>` |

禁止静默吞错、泄露敏感信息或在不可恢复错误上无限重试。

## 8. 日志、指标与审计

定义日志级别、结构化字段、关联ID、敏感字段脱敏、指标命名、审计事件和禁止记录的内容。

## 9. 并发与异步

规定共享状态、锁顺序、超时、取消、线程安全、重复消息、幂等、背压和资源释放。

## 10. 数据访问

规定参数化查询、事务边界、批处理、分页、N+1查询、连接管理、迁移脚本和数据脱敏。

## 11. 安全编码

覆盖输入验证、输出编码、认证授权、密钥、加密、文件与命令处理、反序列化、依赖漏洞和最小权限。

## 12. 性能与资源

规定复杂度、内存、I/O、连接池、缓存、批量操作、资源关闭和性能退化证据要求。

## 13. 注释与开发文档

注释解释“为什么”和约束，不重复代码行为；公开接口、复杂算法、兼容性处理和临时方案必须说明。

## 14. 测试规则

定义单元测试位置、命名、隔离、可重复性、断言、测试数据、覆盖率门槛及禁止依赖真实外部环境。

## 15. 自动化质量门禁

| 门禁 | 工具 | 阈值 | 阻断级别 | 例外审批 |
|---|---|---|---|---|
| 格式化/静态分析/测试/依赖扫描等 | `<工具>` | `<阈值>` | `<级别>` | `<流程>` |

## 16. 规范例外

| 例外编号 | 规则 | 原因 | 风险 | 批准人 | 到期日 | 消除计划 |
|---|---|---|---|---|---|---|
| `CODE-EX-001` | `<规则>` | `<原因>` | `<风险>` | `<人>` | `<日期>` | `<计划>` |

