# UT单元测试设计文档

文档编号：`DEV-UT-001`  
版本：`v0.1`  
负责人：`<姓名>`  
被测版本/提交：`<版本或SHA>`  
测试框架：`<框架及版本>`

## 1. 测试目标与范围

| 被测单元 | 职责 | 关联需求/设计 | 纳入范围 | 排除内容及理由 |
|---|---|---|---|---|
| `<类/函数/模块>` | `<职责>` | `<编号>` | `<范围>` | `<说明>` |

## 2. 测试策略

- 测试层级：`单元/组件内`；
- 隔离边界：`<外部系统、数据库、时钟等>`；
- 设计方法：`等价类/边界值/判定表/状态转换/性质测试等`；
- 风险优先级：`<高风险逻辑>`。

## 3. 测试结构与命名

定义测试文件位置、命名、Arrange-Act-Assert或Given-When-Then结构、共享Fixture和清理规则。

## 4. 依赖替代

| 依赖 | 替代方式 | 行为配置 | 验证交互 | 不替代理由 |
|---|---|---|---|---|
| `<依赖>` | Mock/Stub/Fake | `<行为>` | `<断言>` | `<说明>` |

## 5. 测试数据

| 数据集 | 用途 | 构造方式 | 敏感数据处理 | 清理方式 |
|---|---|---|---|---|
| `<数据>` | `<用途>` | Builder/Fixture等 | `<方式>` | `<方式>` |

## 6. 测试用例设计

| UT编号 | 被测行为 | 场景类型 | 输入/初始状态 | 预期结果 | 预期交互/副作用 | 关联项 |
|---|---|---|---|---|---|---|
| `UT-001` | `<行为>` | 正常/异常/边界 | `<输入>` | `<结果>` | `<交互>` | `<需求/设计>` |

## 7. 状态、时间与并发

说明状态隔离、虚拟时钟、随机数固定、线程调度、竞争条件、超时和取消的测试方法。

## 8. 错误与恢复测试

| 故障 | 注入方式 | 期望错误 | 重试/补偿 | 可观察证据 |
|---|---|---|---|---|
| `<故障>` | `<方法>` | `<错误>` | `<行为>` | `<日志/指标/状态>` |

## 9. 覆盖率目标

| 范围 | 行覆盖 | 分支覆盖 | 条件覆盖 | 关键路径覆盖 | 说明 |
|---|---:|---:|---:|---:|---|
| `<模块>` | `<%>` | `<%>` | `<%>` | 100%/目标值 | `<理由>` |

覆盖率用于发现缺口，不能代替有效断言和风险覆盖。

## 10. 执行环境与命令

- 环境：`<本地/CI>`
- 依赖版本：`<版本>`
- 执行命令：`<命令>`
- 结果位置：`<链接/路径>`

## 11. 执行结果

| 构建/提交 | 用例总数 | 通过 | 失败 | 跳过 | 覆盖率 | 证据 |
|---|---:|---:|---:|---:|---:|---|
| `<ID>` | `<数>` | `<数>` | `<数>` | `<数>` | `<%>` | `<链接>` |

## 12. 未覆盖与已知限制

| 项目 | 原因 | 风险 | 补充测试计划 | 责任人 |
|---|---|---|---|---|
| `<内容>` | `<原因>` | `<风险>` | `<计划>` | `<人>` |

