# 发布说明

文档编号：`REL-NOTES-001`  
产品/系统：`<名称>`  
版本：`<版本>`  
发布日期：`<日期>`  
适用环境：`<环境>`

## 1. 发布摘要

用面向使用者的语言说明本次发布带来的主要价值和影响。

## 2. 新增功能

| 功能 | 用户价值 | 使用方式 | 适用角色 | 关联需求 |
|---|---|---|---|---|
| `<功能>` | `<价值>` | `<说明>` | `<角色>` | `<编号>` |

## 3. 功能变更

| 变更 | 变更前 | 变更后 | 用户影响 | 是否需要操作 |
|---|---|---|---|---|
| `<内容>` | `<之前>` | `<之后>` | `<影响>` | `<动作>` |

## 4. 问题修复

| 问题编号 | 问题摘要 | 影响范围 | 修复结果 |
|---|---|---|---|
| `<编号>` | `<摘要>` | `<范围>` | `<结果>` |

## 5. API、数据与配置变更

| 类型 | 对象 | 变化 | 兼容性 | 消费者/运维动作 | 迁移期限 |
|---|---|---|---|---|---|
| API/数据库/配置 | `<对象>` | `<变化>` | 兼容/破坏 | `<动作>` | `<期限>` |

## 6. 安全与权限变化

说明认证、授权、角色、审计、隐私或安全修复的变化；不得披露可被利用的敏感细节。

## 7. 升级说明

1. `<前置条件>`
2. `<升级动作>`
3. `<验证动作>`

## 8. 回滚说明

- 可回滚版本：`<版本>`
- 回滚入口：`<方案引用>`
- 数据限制：`<限制>`
- 回滚后验证：`<方法>`

## 9. 已知问题与规避

| 问题 | 影响 | 触发条件 | 临时规避 | 预计处理版本 |
|---|---|---|---|---|
| `<问题>` | `<影响>` | `<条件>` | `<方法>` | `<版本>` |

## 10. 验证与追溯

| 需求/缺陷 | PR/MR/Commit | 构建 | 测试报告 | 发布制品 |
|---|---|---|---|---|
| `<编号>` | `<标识>` | `<ID>` | `<文档>` | `<制品>` |

## 11. 支持与反馈

- 支持渠道：`<渠道>`
- 值守时间：`<时间>`
- 严重问题升级：`<方式>`
- 反馈入口：`<入口>`

