# MySQL上线验证清单

文档编号：`REL-MYSQL-001`  
目标实例/集群：`<标识>`  
MySQL版本：`<实际版本>`  
变更编号：`<编号>`  
DBA负责人：`<姓名>`

本模板必须按目标MySQL版本的官方手册校准。非MySQL项目应替换为对应数据库清单，不得机械套用。

## 1. 版本与基础参数

| 检查项 | 变更前 | 目标/期望 | 结果 | 证据 |
|---|---|---|---|---|
| 版本、存储引擎、字符集、排序规则、时区、SQL Mode | `<值>` | `<值>` | `<结果>` | `<证据>` |

## 2. Schema差异

| 对象 | 变更类型 | 当前定义 | 目标定义 | 兼容性 | 风险 |
|---|---|---|---|---|---|
| `<表/列/索引>` | 新增/修改/删除 | `<定义>` | `<定义>` | `<说明>` | `<风险>` |

## 3. 迁移脚本复核

| 脚本 | 校验和 | 幂等 | 预计时长 | 锁影响 | 日志与断点 | 回滚/前滚 |
|---|---|---|---:|---|---|---|
| `<脚本>` | `<哈希>` | 是/否 | `<时长>` | `<影响>` | `<方式>` | `<方案>` |

## 4. 备份与恢复

| 检查项 | 目标 | 实际 | 结果 | 证据 |
|---|---|---|---|---|
| 备份完成、完整性校验、恢复演练、PITR/binlog、RPO/RTO | `<目标>` | `<实际>` | `<结果>` | `<证据>` |

未经恢复验证的备份不得标记为“可恢复”。

## 5. 索引与执行计划

| SQL/场景 | 数据规模 | 当前计划 | 目标计划 | 预计扫描行 | 延迟目标 | 结果 |
|---|---:|---|---|---:|---:|---|
| `<场景>` | `<数量>` | `<摘要>` | `<摘要>` | `<行>` | `<ms>` | `<结果>` |

## 6. 事务、锁与并发

检查事务范围、隔离级别、元数据锁、长事务、死锁、批量大小、在线DDL能力和超时策略。

| 场景 | 并发模型 | 锁风险 | 保护措施 | 观察查询/指标 | 结果 |
|---|---|---|---|---|---|
| `<场景>` | `<模型>` | `<风险>` | `<措施>` | `<指标>` | `<结果>` |

## 7. 用户与权限

| 账号/角色 | 用途 | 当前权限 | 目标最小权限 | 变更 | 验证 |
|---|---|---|---|---|---|
| `<账号>` | `<用途>` | `<权限>` | `<权限>` | `<变更>` | `<结果>` |

检查凭据存储、TLS、管理账号、审计和临时权限到期。

## 8. 复制、集群与高可用

| 检查项 | 变更前 | 变更后 | 阈值 | 结果 |
|---|---|---|---|---|
| 复制状态、延迟、GTID、拓扑、只读、故障转移等 | `<值>` | `<值>` | `<阈值>` | `<结果>` |

## 9. 容量与性能基线

| 指标 | 当前 | 发布阈值 | 发布后 | 结果 |
|---|---:|---:|---:|---|
| 磁盘、连接、QPS、慢查询、缓存命中、复制延迟等 | `<值>` | `<值>` | `<值>` | `<结果>` |

## 10. 上线执行记录

| 序号 | 操作 | 开始/结束 | 执行人 | 影响行数 | 结果 | 证据 |
|---:|---|---|---|---:|---|---|
| 1 | `<操作>` | `<时间>` | `<人>` | `<数量>` | `<结果>` | `<证据>` |

## 11. 上线后数据验证

| 业务对象/查询 | 预期 | 实际 | 抽样/全量 | 结果 | 负责人 |
|---|---|---|---|---|---|
| `<对象>` | `<预期>` | `<实际>` | `<范围>` | `<结果>` | `<人>` |

## 12. 回滚验证

- 回滚触发条件：`<条件>`
- 数据兼容窗口：`<时间>`
- 回滚脚本及校验和：`<引用>`
- 回滚后验证：`<查询/业务检查>`
- 不可逆数据处理：`<方案>`

## 13. DBA结论

- 结论：`允许上线/有条件允许/不允许`
- 条件与风险：`<说明>`
- DBA签署及时间：`<记录>`

